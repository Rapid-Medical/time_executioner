# Hash-pinned pip, installed before anything else in CI.
#
# pylock.toml support requires pip >= 25.1, and the runner's bundled pip is
# not something this repo controls. Installing a known pip with
# --require-hashes makes the rest of the install chain deterministic.
#
# Regenerate with:
#   echo pip > requirements-bootstrap.in
#   uv pip compile requirements-bootstrap.in --generate-hashes --universal \
#       --python-version 3.11 -o requirements-bootstrap.txt
pip==26.2.1 \
    --hash=sha256:71138adf1f4ca900cdb7d289c21b7494329f2332b6d85f0e1c42108c0384ed3e \
    --hash=sha256:f6ad667e89a1fe78046c8f13232b247200f5258d7828f3f7883d660878e0813f
