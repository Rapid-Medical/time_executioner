from setuptools import find_packages, setup

setup(
    name='time_executioner',
    packages=find_packages(),
    version='0.0.1',
    description='Common library to time the execution of functions',
    author='Rapid Medical',
)
