[tox]
envlist = py311

[testenv]
deps =
    pytest
    pytest-cov
    -r requirements.txt
commands = pytest --cov=src --cov-report=xml --cov-config=tox.ini --cov-branch

[coverage:run]
relative_files = True
source = src
